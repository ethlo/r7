# Fail the install outright on an unsupported Node rather than warning and carrying on.
# npm bundled with Node < 20.19 writes an incomplete lockfile here -- it drops rolldown's
# optional platform bindings, and the next `vite build` dies on a missing native binary.
engine-strict=true
