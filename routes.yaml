routes:
  - id: performance test to nginx
    uri:
      - http://localhost:1111
    match:
      - pathStartsWith: /hello
      - method:
          - GET
          - POST
    audit:
      request: HEADERS
      response: FULL
    filters:
      - type: AddResponseHeader
        args:
          name: X-Gateway
          value: Venturi
      - type: AddResponseHeader
        args:
          name: Second-Header
          value: Here
  - id: external-test
    uri:
      - https://www.nrk.no
    match:
      or:
        - pathStartsWith: /v1
    filters:
      - type: StripPrefix
        args:
          parts: 1